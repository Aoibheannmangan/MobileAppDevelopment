<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="32dp">

    <TextView
        android:id="@+id/deviceText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="60dp" />

    <TextView
        android:id="@+id/batteryText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/deviceText"
        app:layout_constraintStart_toStartOf="@id/deviceText"
        app:layout_constraintEnd_toEndOf="@id/deviceText" />

    <TextView
        android:id="@+id/networkText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/batteryText"
        app:layout_constraintStart_toStartOf="@id/batteryText"
        app:layout_constraintEnd_toEndOf="@id/batteryText" />

    <TextView
        android:id="@+id/statusText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:textColor="@android:color/darker_gray"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toBottomOf="@id/networkText"
        app:layout_constraintStart_toStartOf="@id/networkText"
        app:layout_constraintEnd_toEndOf="@id/networkText" />

</androidx.constraintlayout.widget.ConstraintLayout>