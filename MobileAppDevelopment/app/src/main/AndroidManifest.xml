<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Madproject">

        <!-- Fully qualified class names needed because activities moved to subpackages -->
        <activity
            android:name="com.example.madproject.ui.login.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.example.madproject.ui.signup.SignupActivity" />
        <activity android:name="com.example.madproject.ui.home.HomeActivity" />
        <activity android:name="com.example.madproject.ui.addword.AddWordActivity" />
        <activity android:name="com.example.madproject.ui.viewwords.ViewWordActivity" />
        <activity android:name="com.example.madproject.ui.quiz.QuizActivity" />
        <activity android:name="com.example.madproject.ui.devicestats.DeviceStatsActivity" />

    </application>

</manifest>
